"""Phase 2 engine adapters: claude, codex, chat.

Import the registry via ``import engines.base as engines`` and the concrete
adapters from their modules. Kept a real package (not a namespace package) so
the deployed layout imports deterministically next to ``bot.py``.
"""
